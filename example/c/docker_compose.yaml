services:
  clang:
    container_name: clang
    image: clang:latest
    tty: true
    build: .
    working_dir: /root
    volumes:
      - ./workdir:/root
    networks:
      - default

networks:
  default:
    driver: bridge
    