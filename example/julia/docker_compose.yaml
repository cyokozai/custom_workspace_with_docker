services:
  julia:
    container_name: julia
    image: julia:latest
    tty: true
    build: .
    working_dir: /root
    volumes:
      - ./workdir:/root
    networks:
      - default
    environment:
      - TZ=Asia/Tokyo

networks:
  default:
    driver: bridge
    